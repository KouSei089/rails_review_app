<%= search_form_for @q, url: posts_path do |f| %>
  <div class="d-flex">
    <%= f.search_field :name_or_description_cont, class: 'form-control me-2', placeholder: "search" %>
    <%= f.submit "search", class: 'btn btn-outline-success me-md-2' %>
  </div>
<% end %>
